{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      // This is the key: it maps @/ to the project root
      // which should correctly resolve your imports like "@/app/lib/stripe"
      "@/*": ["./*"]
    },
    // ... (rest of your compiler options)
  },
  // ... (rest of the file)
}